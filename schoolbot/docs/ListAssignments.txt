ListAssignments
++listassignments, ++assignments
`class number`
The `ListAssignments` command is intended to list every assignment given a class number or typing the command in a classes given textchannel.
`++assignments 220937 or ++assignments`